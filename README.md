1. What is the difference between var, let, and const?
Ans: Var is available in entire function ,even out side of block and it used to globally. using var facing some bug that why developer avoid using var.

Let and const are same but let value are changeable.

Const value was fixed it can't changed late .If const used {} block it will use only under the block.

------------

2.  What is the spread operator (...)?
Ans: Spread operator (...) is using to expand array and object data.It will separate all data.

-----------

3. What is the difference between map(), filter(), and forEach()?

Ans: forEach() is works like a loop. If will work for every data but if can't return anything.

map() create new array and change array data but it can't change original array.

filter() check a condition if condition are fulfilled it creates a new array.

------------

4.  What is an arrow function?
Ans: Arrow function is sorter way to write a function in javascript.It uses => sign and doesn't need the function keyword.If  arrow funtion are single line it will return value automatically 

------------

5.   What are template literals?
Ans: Template literals is a way to write a string using backtick(` `).It can use to write multiple line code and pass variable under dollar sign and parenthesis ${}.
